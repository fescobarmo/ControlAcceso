# ControlAcceso - Versionado del Sistema
# Este archivo define las versiones de todos los componentes del sistema

# Versión principal del sistema
SYSTEM_VERSION=1.0.0

# Versiones de los servicios Docker
BACKEND_VERSION=1.0.0
FRONTEND_VERSION=1.0.0
DATABASE_VERSION=1.0.0

# Versiones de imágenes base
NODE_VERSION=18-alpine
POSTGRES_VERSION=15-alpine
NGINX_VERSION=1.25-alpine

# Versión de Docker Compose
COMPOSE_VERSION=3.8

# Build timestamp (se actualiza automáticamente)
BUILD_DATE=2024-01-15T10:00:00Z

# Git commit hash (se actualiza automáticamente)
GIT_COMMIT=initial

# Entorno de despliegue
ENVIRONMENT=development
